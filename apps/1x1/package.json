{
  "name": "1x1",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "vite build",
    "cy:open": "cypress open --e2e --browser chrome",
    "cy:run": "cypress run --e2e --quiet --browser chrome",
    "dev": "vite",
    "pw:open": "playwright test --ui",
    "pw:run": "playwright test",
    "preview": "vite preview",
    "run": "vite build && vite preview",
    "test": "vitest --watch=false --silent=passed-only --reporter=minimal",
    "test:cov": "vitest --watch=false --silent=passed-only --coverage"
  },
  "devDependencies": {
    "fast-check": "^4.10.0"
  },
  "dependencies": {
    "@flashcards/shared": "workspace:*",
    "@quasar/extras": "^2.1.0",
    "quasar": "^2.32.3",
    "vue": "^3.5.42",
    "vue-router": "^5.3.1"
  }
}
